# `color-tracking` — Orin Color Tracking

Standalone color-tracking experiment targeted at the Jetson Orin.

Contains `color_tracking_orin.py`, a self-contained color-tracking script. It was split out of the base onto its own branch to keep it separate from the cube-detector pipeline, and it is intentionally **not** part of the `cube-detector-v1` milestone.
